To start the backend server:
cd backend ->
npm install ->
npm run dev ->
Backend will start :
 http://localhost:5001

To start the Frontend, open a new terminal:
cd frontend->
npm install->
npm run dev->
Frontend will start :
 http://localhost:5173

